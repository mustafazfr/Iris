import 'package:flutter/material.dart';

class AppColors {
  // Uygulamanızın ana renk paleti
  static const Color primary = Color(0xFF5271FF); // Ana renk (mor tonu)
  static const Color primaryLight = Color(0xFFBB86FC); // Ana rengin daha açık tonu
  static const Color primaryDark = Color(0xFF3700B3); // Ana rengin daha koyu tonu

  static const Color secondary = Color(0xFF03DAC6); // Vurgu rengi (turkuaz tonu)
  static const Color secondaryLight = Color(0xFF66FFF9); // Vurgu rengin daha açık tonu
  static const Color secondaryDark = Color(0xFF018786); // Vurgu rengin daha koyu tonu

  static const Color primaryColor = Color(0xFF5271FF); // Başlıktaki ve alttaki çubuktaki bordo/kırmızı
  static const Color accentColor = Color(0xFF5271FF); // Canlı pembe/kırmızı vurgu (belki buton içi)
  static const Color backgroundColorLight = Color(0xFFF9F9F9); // Açık gri arka plan
  static const Color backgroundColorDark = Color(0xFFF0F0F0); // Biraz daha koyu gri arka plan
  static const Color cardColor = Color(0xFFFFFFFF); // Beyaz kart
  static const Color textFieldFillColor = Color(0xFFFFFFFF); // Input içi beyaz
  static const Color borderColor = Color(0xFFE0E0E0); // Gri border
  static const Color textColorDark = Color(0xFF212121); // Koyu gri metin
  static const Color textColorLight = Color(0xFF757575); // Açık gri metin
  static const Color iconColor = Color(0xFF9E9E9E); // Gri ikon
  static const Color starColor = Color(0xFFFFD700); // Sarı yıldız rengi (Altın sarısı)
  static const Color tagColorActive = Color(0xFFE2004F); // Aktif etiket (Kırmızı)
  static const Color tagColorPassive = Color(0xFFCCCCCC); // Pasif etiket (Gri)
  static const Color dividerColor = Color(0xFFEEEEEE); // Ayırıcı çizgiler



  // Metin renkleri
  static const Color textPrimary = Color(0xFF212121); // Koyu metin rengi
  static const Color textSecondary = Color(0xFF757575); // Açık gri metin rengi
  static const Color textOnPrimary = Colors.white; // Primary rengi üzerindeki metin (genellikle beyaz)
  static const Color textOnError = Colors.white; // Hata rengi üzerindeki metin (genellikle beyaz)

  // Durum renkleri
  static const Color error = Color(0xFFB00020); // Hata rengi
  static const Color success = Color(0xFF4CAF50); // Başarı rengi (yeşil)
  static const Color warning = Color(0xFFFB8C00); // Uyarı rengi (turuncu)

  // Arka plan ve yüzey renkleri
  static const Color background = Color(0xFFF5F5F5); // Genel arka plan rengi (açık gri)
  static const Color surface = Colors.white; // Kartlar, dialoglar gibi yüzey rengi

  // Gri tonlar (isteğe bağlı)
  static const Color greyLight = Color(0xFFE0E0E0);
  static const Color greyMedium = Color(0xFF9E9E9E);
  static const Color greyDark = Color(0xFF616161);

// Marka özel renkler (örneğin logo renkleri, eğer varsa)
// static const Color brandBlue = Color(0xFF1A73E8);
// static const Color brandGreen = Color(0xFF34A853);
}